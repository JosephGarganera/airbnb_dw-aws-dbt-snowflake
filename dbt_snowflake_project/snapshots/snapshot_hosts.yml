snapshots:
  - name: snapshot_hosts
    relation: source('staging', 'hosts')
    config:
      schema: gold
      database: "{{ target.database }}"
      unique_key: host_id
      strategy: timestamp
      updated_at: created_at

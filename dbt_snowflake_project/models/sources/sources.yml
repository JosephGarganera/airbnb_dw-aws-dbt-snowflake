sources:
  - name: staging
    database: AIRBNB
    schema: staging

    config:
      freshness:
        warn_after: { count: 12, period: hour }
        error_after: { count: 24, period: hour }
      loaded_at_field: created_at

    tables:
      - name: listings
      - name: bookings
      - name: hosts
